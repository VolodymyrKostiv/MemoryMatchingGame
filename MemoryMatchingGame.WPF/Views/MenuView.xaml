<UserControl x:Class="MemoryMatchingGame.WPF.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MemoryMatchingGame.WPF.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MemoryMatchingGame.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:MenuViewModel,
                                              IsDesignTimeCreatable=True}"
             mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Vertical">
            <Button Content="Continue" />
            <Button Command="{Binding StartNewGameCommand}" Content="Start New Game" />
            <Button Command="{Binding OpenSettingsCommand}" Content="Settings" />
            <Button Command="{Binding ExitGameCommand}" Content="Exit Game" />
        </StackPanel>
    </Grid>
</UserControl>
