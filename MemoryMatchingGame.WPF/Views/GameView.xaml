<UserControl x:Class="MemoryMatchingGame.WPF.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MemoryMatchingGame.WPF.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:entities="clr-namespace:MemoryMatchingGame.Core.Entities;assembly=MemoryMatchingGame.Core"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:usercontrols="clr-namespace:MemoryMatchingGame.WPF.UserControls"
             xmlns:vm="clr-namespace:MemoryMatchingGame.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:GameViewModel,
                                              IsDesignTimeCreatable=True}"
             mc:Ignorable="d">
    <Grid x:Name="parentGrid"
          Margin="{Binding Columns}"
          HorizontalAlignment="Center">
        <ItemsControl Width="Auto"
                      Height="Auto"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ItemsSource="{Binding Cards}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding DataContext.Columns, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Rows="{Binding DataContext.Rows, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <!--<Setter Property="Height" Value="100" />
                    <Setter Property="Width" Value="100" />-->
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <usercontrols:ContentControl3D x:Name="contentCtrl3D"
                                                   HorizontalAlignment="Stretch"
                                                   VerticalAlignment="Stretch">
                        <usercontrols:ContentControl3D.Content>
                            <Button HorizontalContentAlignment="Stretch" Command="usercontrols:ContentControl3D.RotateCommand">
                                <Image CacheMode="BitmapCache"
                                       RenderOptions.BitmapScalingMode="LowQuality"
                                       SnapsToDevicePixels="True"
                                       Source="{Binding DataContext.BackImage, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       Stretch="Fill" />
                            </Button>
                        </usercontrols:ContentControl3D.Content>
                        <usercontrols:ContentControl3D.BackContent>
                            <Button HorizontalContentAlignment="Stretch" Command="usercontrols:ContentControl3D.RotateCommand">
                                <Image CacheMode="BitmapCache"
                                       RenderOptions.BitmapScalingMode="LowQuality"
                                       SnapsToDevicePixels="True"
                                       Source="{Binding Image}"
                                       Stretch="Fill" />
                            </Button>
                        </usercontrols:ContentControl3D.BackContent>
                    </usercontrols:ContentControl3D>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
